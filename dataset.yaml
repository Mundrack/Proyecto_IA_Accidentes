# Rutas a tus carpetas de imágenes (usamos barras normales /)
# Para este prototipo, usaremos la misma carpeta para entrenar y validar
# path: Carpeta raíz de tus datos. Indica el directorio principal en tu PC que contiene la base de datos completa.
# Tras tu limpieza, ahora apunta a la carpeta raíz simplificada Data-Guardada.
path: C:/Users/MateoGPugaM/OneDrive - Grupo Radical/Escritorio/Proyecto_IA_Accidentes/Data-Guardada

# train: Carpeta con las imágenes que la IA usará para aprender (ruta relativa a la raíz 'path' indicada arriba).
train: head_on_collision

# val: Carpeta con las imágenes que la IA usará para validar su aprendizaje (ruta relativa a la raíz 'path' de arriba).
val: head_on_collision

# Número de clases
nc: 1

# Nombres de las clases (DEBE coincidir con lo que pusiste en MakeSense)
names: ['accidente']